
/*it means include information about 
 * standard library*/


/*it means,define a fucntion called main
 * that received no argument values*/
int main() 
{ /*statement of main are enclosed in braces*/
	
	
        /*main calls library function printf to print this 
	 * sequence of characters
	 * \n represents the newline character*/
	printf("hello cwbea\n");

	/*returns 0 hence everything works fine
	 * semicolon means end of a function or variable
	 * semicolon also means the end of a statement*/
	return 0;
}
